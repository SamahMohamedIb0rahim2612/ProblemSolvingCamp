class Solution {
public:
    void reverseString(vector<char>& s) {
        int I=0, r=s.size()-1;

        while(I<r) {
            std::swap(s[I],s[r]);  
            I++;
            r--;
        }
    }
};
